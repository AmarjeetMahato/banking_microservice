spring.application.name=auth


spring.security.oauth2.client.registration.google.client-id=YOUR_CLIENT_ID

spring.security.oauth2.client.registration.google.client-secret=YOUR_CLIENT_SECRET

spring.security.oauth2.client.registration.google.scope=email,profile


app.cors.front-end-url=http://localhost:3000

app.jwt.secret=9a4f2c8d3b7a1e6f45c8a0b3f267d8b1d4e6f3c8a9d2b5f8e3a9c8b5f6v8s3n2
app.jwt.access-ttl-seconds=900
app.jwt.refresh-ttl-seconds=86400
app.jwt.issuer=my-auth-service



# ===================================================================
# PostgreSQL Database Configuration
# ===================================================================
spring.datasource.url=jdbc:postgresql://localhost:5432/banking_microservice
spring.datasource.username=postgres
spring.datasource.password=root123
spring.datasource.driver-class-name=org.postgresql.Driver

# ===================================================================
# JPA / Hibernate Configuration
# ===================================================================
# Shows the SQL queries in your console (Great for debugging)
spring.jpa.show-sql=true

# Formats the SQL queries in the console so they are readable
spring.jpa.properties.hibernate.format_sql=true

# Automatically updates your database schema based on your @Entity classes
# Options: create, create-drop, validate, update, none
spring.jpa.hibernate.ddl-auto=update

# Specifies the dialect so Hibernate writes optimized queries for Postgres
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===================================================================
# Your Custom Application Properties
# ===================================================================
app.mail.from=amarjitmahato016@gmail.com
app.mail.from-name=Your App Name

# Optional: Overriding the defaults (10 and 30) defined in your Java code
app.mail.otp-expiry-minutes=15
app.mail.suspicious-login-expiry-minutes=45

# ===================================================================
# Spring Mail Configuration (Required for JavaMailSender to connect)
# ===================================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=amarjitmahato016@gmail.com
spring.mail.password=lfek dvri pakl sybj
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


